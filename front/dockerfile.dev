FROM node:22-alpine

RUN npm install -g pnpm

WORKDIR /app

COPY package.json pnpm-lock.yaml ./

RUN pnpm install

# Source is mounted via volume — no COPY . .
# --host exposes Vite on 0.0.0.0 so Docker port mapping works
CMD ["pnpm", "run", "dev"]
